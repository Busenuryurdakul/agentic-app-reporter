apiVersion: v1
kind: Service
metadata:
  name: masterfabric-api
  namespace: masterfabric
  labels:
    app.kubernetes.io/name: masterfabric-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: masterfabric-api
  ports:
    - name: http
      port: 8080
      targetPort: http
